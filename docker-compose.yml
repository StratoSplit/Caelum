services:
  audio_dashboard:
    build: ./app
    working_dir: /usr/src/app
    volumes:
      - ./app:/usr/src/app
    ports:
      - "8000:8000"

  audio_stream:
    build: ./audio_stream
    working_dir: /usr/src/audio_stream
    volumes:
      - ./audio_stream:/usr/src/audio_stream